<%@plots.each do |plot|%>
<div id = "plot-<%=plot.id%>">
<h2>Plot Number: <%=plot.number%></h2>
  Plants: <%plot.plants.each do |plant|%>
    <p><%=link_to plant.name, "plots/#{plot.id}/plants/#{plant.id}", method: :delete %></p>
  <%end%>
</div>  
<%end%>